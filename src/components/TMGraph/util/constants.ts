// src/components/TMGraph/util/constants.ts
export const STATE_NODE_DIAMETER = 64;

// Height of the control buttons in the TM graph
export const CONTROL_HEIGHT = 32;

export enum EdgeType {
  FLOATING = 'floating',
  LOOP = 'loop',
}

export enum NodeType {
  STATE = 'state',
}
